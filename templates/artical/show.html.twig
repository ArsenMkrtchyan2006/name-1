{% extends 'base.html.twig' %}

{% block title %}Artical{% endblock %}

{% block body %}
    <h1>Artical</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artical.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ artical.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ artical.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ artical.createdAt ? artical.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>IsEnabled</th>
                <td>{{ artical.isEnabled ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('artical_index') }}">back to list</a>

    <a href="{{ path('artical_edit', {'id': artical.id}) }}">edit</a>

    {{ include('artical/_delete_form.html.twig') }}
{% endblock %}
